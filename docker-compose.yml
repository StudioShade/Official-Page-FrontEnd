services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: studio-shade-web
    ports:
      - "43001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped